# IP 화이트리스트 설정 (관리자 전용)
# /usr/local/nginx/conf/ip_whitelist.conf

# 허용할 관리자 IP 목록
# 실제 관리자 IP로 변경하세요!

# 로컬호스트
allow 127.0.0.1;
allow ::1;

# 관리자 IP (예시 - 실제 IP로 변경)
# allow 1.2.3.4;        # 관리자 1
# allow 5.6.7.8;        # 관리자 2  
# allow 10.0.0.0/24;    # 내부 네트워크

# 나머지 모든 IP 차단
deny all;